#include<iostream>
#include<ctime>
#include<cstdlib>
using namespace std;
main(){
	srand(time(NULL));
	int a = rand() % 1000;
	int b;
	int i;
	cout<<"Please input your number: ";
	do{
		cin>>b;
		if(a > b){
			cout<<"The number is higher,try again: ";
		}
		if(a < b){
			cout<<"The number is lower,try again: ";
		   }
   }while(a != b);
   cout<<"Great, the number is: "<<a;
}
